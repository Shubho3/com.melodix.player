package com.melodix.player.model

/** The three user-picked colors (packed ARGB ints) a custom theme is derived from. */
data class CustomThemeColors(
    val accent: Int,
    val background: Int,
    val text: Int,
)
